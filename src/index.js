import React from 'react';
import ReactDOM from 'react-dom/client';
import { OldApp } from 'components/App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OldApp />
  </React.StrictMode>
);
